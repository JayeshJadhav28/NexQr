'use client';

import React from 'react';
import MobileNavigation from './MobileNavigation';

const ClientMobileNavWrapper: React.FC = () => {
  return <MobileNavigation />;
};

export default ClientMobileNavWrapper;
